'use server';

import { AppLayout } from '@/components/dashboard/dashboard-client';

export default async function ReportsPage() {
  return (
    <AppLayout>
      {/* The content of this page has been cleared as per your request. */}
    </AppLayout>
  );
}
